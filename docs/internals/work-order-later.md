# Later work order phases

These phases return to `WORK-ORDER.md` when REL-001 is ready to start.

## ASM, assemblies

| ID | Outcome | Status | Depends | Scope | Check |
| --- | --- | --- | --- | --- | --- |
| ASM-001 | Document assemblies beside part documents using KIT rigid placements. Components follow DEC-602: the latest revision plus an acknowledged revision for the update badge. Define per-document cache identity, joint frames, nested-instance paths, BOM and interference derivation, and an approved assembly workbench design. Exclude in-context editing, motion studies, contact and cross-project components. | todo | DOC-CP, KIT-CP | `docs/internals/assemblies.md` | the note exists and names every type and rule in this Outcome; `scripts/check-work-order.sh` passes |
| DEC-603 | Mark approves the assembly workbench wireframe in `docs/internals/assemblies.md`, as the UI rows below need an approved design. | decision | ASM-001 | `WORK-ORDER.md` | the approval is recorded under Rulings |
| ASM-002 | `shared/src/assembly.ts` exports `AssemblyDocument`, `ComponentRef`, `Instance` and `validateAssembly` built on the KIT runtime schema approach and placement type. `validateAssembly` rejects duplicate instance ids, a non-rigid placement matrix (non-orthonormal rotation), a `ComponentRef` to the assembly itself, and a missing `documentId`. Not: joints (ASM-009). | todo | ASM-001, DEC-602, KIT-027 | `shared/src/assembly.ts`, `shared/src/index.ts`, `shared/test/assembly.test.ts` | `npm test -w shared -- assembly` rejects, each with its path, duplicate ids, a rotation column of length 1.01, a reflection (determinant -1), a self reference and a missing `documentId`, and accepts a valid two-instance assembly |
| ASM-003 | The server stores assemblies as a DOC document kind with validate-on-load and revisions. Routes under the KIT shared route contract create an assembly and add, update and remove instances as UNDO server transactions. A `ComponentRef` to a document that does not exist or is not a part returns 400. Name document-scoped routes and keep a consistent project manifest. Each mutation and history entry identifies its document; undo cannot restore one document over another. | todo | ASM-002, DOC-CP, UNDO-CP | `server/src/assembly/store.ts`, `server/src/assembly/routes.ts`, the route registration file, `server/test/assemblyStore.test.ts` | `npm test -w server -- assemblyStore`; a missing or non-part ref gets 400, a stale `If-Match` 409, an outsider 404 and a corrupt stored assembly 422 on load; undo in document B cannot restore an entry from document A |
| ASM-004 | `server/src/assembly/graph.ts` exports `AssemblyGraph` with `dependents(documentId)` and `evaluateAssembly(assemblyId)`. It evaluates each referenced part once at its end-of-timeline state through the PERF kernel worker, caches bodies per documentId and revision, and returns instances with world placements without copying shapes. Editing part A re-evaluates only A. | todo | ASM-003, PERF-CP | `server/src/assembly/graph.ts`, `server/test/assemblyGraph.test.ts` | `npm test -w server -- assemblyGraph`: with parts A and B in one assembly, a change to A calls the evaluator once for A and zero times for B |
| ASM-005 | `GET` assembly scene route returns `{components: [{key, documentId, revision, meshHash}], instances: [{id, key, matrix}]}`. Meshes come from the PERF content-hashed binary mesh endpoint, so a part used 100 times ships one mesh and 100 matrices. | todo | ASM-004, PERF-CP | `server/src/assembly/routes.ts`, `shared/src/assembly.ts`, `server/test/assemblyScene.test.ts` | `npm test -w server -- assemblyScene`: 100 instances of one part give one `meshHash` and 100 matrices |
| ASM-006 | Opening an assembly document shows a registered `assembly` workbench: the viewport draws one `THREE.Mesh` per instance, sharing its component geometry, and a tree panel lists instances with name, component, grounded and visibility. Selecting a row selects the instance as selection kind `asm.instance` and highlights it. Visibility is view state, outside undo. | todo | ASM-005, REG-CP, OPS-CP, DEC-603 | `client/src/assembly/*`, `client/test/assemblyTree.test.ts` | `npx vitest run client/test/assemblyTree.test.ts` |
| ASM-007 | Command `asm.insertPart` opens a picker of the project's part documents (never the open assembly) and adds an instance at the origin. The first instance is grounded. Undo removes it. | todo | ASM-006, UNDO-CP | `client/src/assembly/*`, `server/src/assembly/routes.ts`, `client/test/assemblyInsert.test.ts` | `npx vitest run client/test/assemblyInsert.test.ts`; the picker excludes the open assembly and other assemblies; a stale insert (409) adds nothing; the first instance is grounded and the second is not; undo removes the instance |
| ASM-008 | Command `asm.move` drags an ungrounded instance with the KIT gizmo base (translate and rotate) and writes `placement` as one transaction on release. A grounded instance refuses with a message. | todo | ASM-007, KIT-CP | `client/src/assembly/*`, `client/test/assemblyMove.test.ts` | `npx vitest run client/test/assemblyMove.test.ts`; a grounded instance refuses without a request; Esc during a drag sends nothing; one release sends one request |
| ASM-009 | `shared/src/assemblyJoints.ts` exports the `Joint` union for the DEC-601 scope and pure `solvePlacements(instances, joints, frames)`. It walks from grounded instances and returns a placement per instance or typed errors `overconstrained`, `unreachable` and `outsideLimits`. An unjointed ungrounded instance keeps its stored placement. | todo | ASM-002, DEC-601 | `shared/src/assemblyJoints.ts`, `shared/src/index.ts`, `shared/test/assemblyJoints.test.ts` | `npm test -w shared -- assemblyJoints`: a property test keeps a rigid child fixed relative to its parent under random parent placements; revolute 90 rotates the child a quarter turn about the joint axis; two joints driving one child give `overconstrained` |
| ASM-010 | `server/src/assembly/jointFrame.ts` resolves a joint origin stored as a REF reference on a part body to a frame: a planar face gives centroid and normal, a circular edge gives centre and axis, a vertex gives the point with part axes. An unresolved reference returns `missing`, not a throw. Assembly evaluation feeds these frames to `solvePlacements`. Other geometry returns `unsupported`, not a throw. | todo | ASM-004, ASM-009, REF-CP | `server/src/assembly/jointFrame.ts`, `server/src/assembly/graph.ts`, `server/test/jointFrame.test.ts` | `npm test -w server -- jointFrame`; a box top face, a hole edge and a vertex give the expected frames; a deleted face gives `missing`; a cylindrical face gives `unsupported` |
| ASM-011 | Command `asm.joint` opens a KIT form-kit dialog: pick an origin on instance A, an origin on instance B, the joint type, value and limits. The value field previews through the UNDO preview supersede, OK commits one transaction, and joint errors show on the joint row in the tree. `asm.move` refuses on a joint-driven instance. | todo | ASM-008, ASM-010 | `client/src/assembly/*`, `client/test/assemblyJoint.test.ts` | `npx vitest run client/test/assemblyJoint.test.ts`; OK stays disabled until both origins are picked; picking the same instance for A and B is refused; an out-of-limits value shows `outsideLimits` and commits nothing; Cancel leaves no history entry; `asm.move` refuses on a joint-driven instance |
| ASM-012 | Implement DEC-602: a component evaluates the current part, stores the acknowledged revision for the `updated` badge, and rechecks joint origins when they differ. Preserve history and asset closure, and report broken joint references without silently changing mates. No pinned mode. It registers a blob enumerator with UNDO-010 so component blobs are kept. | todo | ASM-006, ASM-010, DEC-602 | `server/src/assembly/*`, `client/src/assembly/*`, `server/test/assemblyUpdate.test.ts` | `npm test -w server -- assemblyUpdate`: a part edit shows `updated` and acknowledging clears it; a deleted origin face reports `missing` and keeps the last placement with no rebinding; a deleted part errors on its component; a `pinnedRevision` field is rejected; the assembly bytes stay unchanged until acknowledged; UNDO-010 keeps component blobs |
| ASM-013 | A `ComponentRef` may point to another assembly. `AssemblyGraph` recurses, rejects a cycle (A contains B contains A) with 400 on insert, and the scene route flattens nested matrices. | todo | ASM-004, ASM-005 | `server/src/assembly/*`, `shared/src/assembly.ts`, `server/test/subassembly.test.ts` | `npm test -w server -- subassembly`; two-cycle and three-cycle references get 400 on both insert and update and the stored documents stay unchanged; nesting deeper than 32 gets 400; a rotated and translated two-level nest flattens in parent-times-child order |
| ASM-014 | `deriveBom(assemblyId)` returns rows `{documentId, name, revision, quantity, level}` from every instance, nested by subassembly. A `GET` BOM route and a `bom.csv` exporter in the EXCH exporter registry serve it. Nothing is written to the document. Hidden instances count, because visibility is view state. | todo | ASM-013, EXCH-CP | `server/src/assembly/bom.ts`, `server/src/assembly/routes.ts`, `server/test/assemblyBom.test.ts` | `npm test -w server -- assemblyBom`: quantities for 2 parts by 3 instances in a subassembly used twice, and the stored document is byte-identical before and after; hiding an instance leaves the quantities unchanged |
| ASM-015 | Kernel job `asm.interference` computes world bounding boxes, tests only overlapping pairs with `BRepAlgoAPI_Common` on located shapes, and reports `{a, b, volume}` above 1e-6 mm3, with progress per pair and PERF cancel. Touching faces report nothing. Results are not stored. | todo | ASM-004, PERF-CP | `server/src/assembly/interference.ts`, `server/test/assemblyInterference.test.ts` | `npm test -w server -- assemblyInterference`: two 10 mm cubes overlapping by 1 mm on each axis report volume 1; face-touching cubes report none |
| ASM-016 | Command `asm.interference` runs the job, lists pairs with volumes in a panel, and draws the overlap solids on a REG viewport layer `asm.interference`. Selecting a pair isolates both instances. | todo | ASM-015, ASM-006 | `client/src/assembly/*`, `client/test/assemblyInterference.test.ts` | `npx vitest run client/test/assemblyInterference.test.ts` |
| ASM-017 | `MeasureRequest` refs gain an optional `instancePath`: the instance ids from the open assembly down to the leaf. The server composes the placements along that path before `measure()` (`server/src/geometry/measure.ts:105`), and the Measure panel works in the assembly workbench. | todo | ASM-005, ASM-006, ASM-013 | `shared/src/api.ts`, `server/src/geometry/measure.ts`, `server/src/assembly/routes.ts`, `client/src/components/MeasurePanel.tsx`, `server/test/assemblyMeasure.test.ts` | `npm test -w server -- assemblyMeasure`: two instances of a 10 mm cube with origins 30 mm apart on X measure a 20 mm face gap; two copies of one subassembly measure and select independently; an unresolvable path gets 400; a leaf-only id inside a nested copy gets 400 |
| ASM-018 | Importing a STEP file with assembly structure through the EXCH XDE importer creates one part document per unique shape and one assembly document with named instances and placements. | todo | ASM-013, EXCH-CP | `server/src/assembly/stepAssembly.ts`, `server/test/fixtures/*.step`, `server/test/stepAssembly.test.ts` | `npm test -w server -- stepAssembly`: a fixture with 2 parts by 3 instances yields 2 part documents and 6 instances at the fixture placements |
| ASM-020 | Repeated component instances that share one mesh hash draw as one `THREE.InstancedMesh` per hash and level, with per-instance matrices from each instance's `Placement` (KIT-015). Picking maps `instanceId` back to the component instance, and highlights use per-instance colour. | todo | PERF-CP, ASM-006 | `client/src/three/bodyObjects.ts`, `client/src/three/CadViewport.ts`, `client/test/instancing.test.ts`, `client/test/viewport.bench.ts` | `npx vitest run client/test/instancing.test.ts`: 1,000 instances of one part create one geometry and one draw call, a pick returns the right instance, and `sync bodies` for that scene is recorded against PERF-022 |
| ASM-019 | Benchmark `asm-large` uses 20 unique parts with 2,000 instances in total. Before measuring, `docs/internals/performance.md` commits budgets on the reference class: cold open median and p95, one-part re-evaluation p95, scene payload at most 128 bytes per instance plus meshes, server RSS, and client frame time while orbiting at 1920x1080 in the OPS-030 browser of median 16.7 ms or less and p95 33 ms or less. ASM-020 owns instancing correctness and the draw-call count; this row owns the scale budgets. | todo | ASM-005, ASM-006, PERF-CP, ASM-020 | the PERF benchmark folder, `server/test/assemblyScale.test.ts`, `docs/internals/performance.md` | `npm test -w server -- assemblyScale` keeps its two assertions; `npm run bench -w server -- asm-large` and the browser lane record median and p95 with 3 warm-up runs and 20 samples; any budget breach fails the row |
| ASM-CP | ASM integration checkpoint: every required dependency has passed its focused proof; npm run check and this phase's aggregate checks pass. Update only the existing documentation whose facts changed, and record a sanitised evidence summary. This is not approval to deploy or a claim that excluded scope is complete. | todo | ASM-001, ASM-002, ASM-003, ASM-004, ASM-005, ASM-006, ASM-007, ASM-008, ASM-009, ASM-010, ASM-011, ASM-012, ASM-013, ASM-014, ASM-015, ASM-016, ASM-017, ASM-018, ASM-019, ASM-020 | `ARCHITECTURE.md`, `CAD_MODEL.md`, `API.md`, `WORK-ORDER.md` | npm run check; scripts/check-work-order.sh; all required phase checks pass with evidence; each excluded row is reported by ID |

## PLUG, plugin packaging and loading

| ID | Outcome | Status | Depends | Scope | Check |
| --- | --- | --- | --- | --- | --- |
| PLUG-001 | `docs/internals/modules.md`, under 150 lines, has seven sections. Layout: `modules/<id>/` with `package.json`, `manifest.json`, `server.ts`, `client.tsx`, `LICENSE` and `test/`; first-party modules are listed by hand in `modules/index.server.ts` and `modules/index.client.ts` and statically imported. Imports and dependencies: module code imports only `@rockett/plugin-api`, its own files, `react`, `react-dom`, `three` and packages its own `package.json` pins exactly; `react`, `react-dom` and `three` are exact `peerDependencies` at the host versions; core services arrive through the activation context, never by importing core or another module. Workspaces: each first-party module is a private workspace `@rockett/module-<id>` under root `modules/*` sharing the root lockfile and `.npmrc`; esbuild and Vite bundle module code and its dependencies, so the runtime image installs no module package, and the Dockerfile copies each module `package.json` before `npm ci`. API versions: additive within a major, one major of deprecation. Disable and hide: what app disable and user hide mean. Missing modules: the document opens editable, the module's features show as errors, and its data is kept as equal JSON values. Third-party loading: prebuilt `server.mjs` and `client.mjs` bundle their own dependencies, and only React, react-dom and three resolve to the host, by the mechanism PLUG-017 records. | todo | REG-CP | `docs/internals/modules.md` | `test "$(wc -l < docs/internals/modules.md)" -lt 150`; a grep for the seven headings Layout, Imports and dependencies, Workspaces, API versions, Disable and hide, Missing modules and Third-party loading counts 7; `npm run lint:writing` passes |
| PLUG-002 | `shared/src/moduleManifest.ts` defines `ModuleManifest` (`manifestVersion: 1`, id, name, version, `apiRange`, licence as an SPDX id, author, `contributes` with features, commands, workbenches, panels, settings, importers, exporters, routes, selection kinds) and `parseManifest(json, hostApiVersion)` built on the KIT runtime schema. It rejects contribution ids that do not start with `<id>.`, accepts only `^MAJOR.MINOR` API ranges, and returns `incompatible` with the reason when the host API is outside the range. The contribution schema must include postProcessors before CAM-025 registers one. Use one namespace convention, plugin.<moduleId>.*, for settings across SET and PLUG. | todo | PLUG-001, KIT-027 | `shared/src/moduleManifest.ts`, `shared/src/index.ts`, `shared/test/moduleManifest.test.ts` | `npm test -w shared -- moduleManifest` (valid manifest parses; bad id prefix, missing licence and a `^2.0` range on a 1.x host each fail with a clear message) |
| PLUG-003 | New private workspace `plugin-api` (`@rockett/plugin-api`, exact version) exports `PLUGIN_API_VERSION`, which stays 0.x until PLUG-006 runs Measure through it; type-only re-exports of every REG contract; `defineServerModule` and `defineClientModule`; and an activation context with narrow runtime services for Measure, CAM and KiCad plus `services.provide(id, fn)` and `services.get(id)` for `<moduleId>.*` ids, where `get` returns undefined when no provider is loaded. Root `workspaces` gains `plugin-api` and `modules/*`; `typecheck`, the client and server path settings and the Dockerfile `package.json` copies include them. Test discovery stays with OPS-010. `server/test/moduleBoundaries.test.ts` scans `modules/` and fails on an import of core paths, of another module, or of a package the module does not pin exactly. | todo | PLUG-002 | `plugin-api/package.json`, `plugin-api/tsconfig.json`, `plugin-api/src/index.ts`, `package.json`, `package-lock.json`, `server/test/moduleBoundaries.test.ts`, `Dockerfile`, `server/test/fixtures/modules/**` | `npm run typecheck`; `npm test -w server -- moduleBoundaries` reports fixtures importing `../server/src`, `../../kicad/src` and an unpinned package, and passes a fixture importing its own pinned dependency; a local `docker build .` passes |
| PLUG-004 | Server module host: `server/src/modules/host.ts` loads each listed module, checks its manifest against `PLUGIN_API_VERSION`, registers its contributions through the REG registries with one disposer per module, and isolates failures: a module that throws is marked `failed` with its message and the rest load. `GET /api/modules` returns id, name, version, licence, author, status (`loaded`, `failed`, `incompatible`, `disabled`) and error. Registration is atomic per module: if any contribution throws, dispose every earlier contribution from that module before reporting failed. | todo | PLUG-003 | `server/src/modules/host.ts`, `server/src/index.ts`, `server/src/api/routes.ts`, `server/test/moduleHost.test.ts` | `npm test -w server -- moduleHost` (a fixture module's route and feature work; a throwing module is `failed` and others still load; disposing a module removes every contribution) |
| PLUG-005 | Client module host: `client/src/modules/host.ts` registers the client parts of loaded modules from `modules/index.client.ts`, skips modules the server reports as not loaded, keeps one disposer per module, and wraps every contributed component in the REG-030 error boundary. | todo | PLUG-004, OPS-CP | `client/src/modules/host.ts`, `client/src/main.tsx`, `client/src/api.ts`, `client/test/moduleHost.test.tsx` | `npx vitest run client/test/moduleHost.test.tsx` in the DOM lane (a fixture module's command appears; one the server reports `failed` does not; a throwing panel does not blank the app) |
| PLUG-006 | Proof of the layout: Measure moves into `modules/measure/` (manifest with licence, `server.ts` registering the measure route module and `measure.ts`, `client.tsx` registering `inspect.measure`, its I key and its panel). The core copies of the measure route, command and panel are deleted. | todo | PLUG-005 | `modules/measure/*`, `modules/index.server.ts`, `modules/index.client.ts`, `server/src/api/measureRoutes.ts`, `client/src/commands/measure.ts`, `client/src/components/MeasurePanel.tsx` | `npm test -w server -- api moduleBoundaries`; `npx vitest run client/test/measureCommand.test.ts` (imports from the module path) |
| PLUG-007 | App-level module enablement is a SETTINGS definition under plugin.<moduleId>.enabled, writable only by admins. It controls server loading at startup; disabled modules register nothing and follow the missing-module data-preservation rule. Enforce the setting on the server through AUTH and SET-017. | todo | PLUG-004, SET-017 | `server/src/modules/host.ts`, `shared/src/settings.ts`, `server/test/moduleHost.test.ts` | `npm test -w server -- moduleHost` (disabling the fixture module removes its route and marks it `disabled`) |
| PLUG-008 | User module visibility is a SETTINGS definition under plugin.<moduleId>.hidden in the user layer. Hide its commands, workbenches, panels and toolbar groups for that user; keep server evaluation and other users unaffected. The host owns these reserved setting names and rejects conflicting module definitions. | todo | PLUG-005, PLUG-007, AUTH-CP | `client/src/modules/host.ts`, `client/src/commands/registry.ts`, `client/src/shell/workbench.ts`, `shared/src/settings.ts`, `client/test/moduleHost.test.tsx` | `npx vitest run client/test/moduleHost.test.tsx` gains a case: a hidden module's command and workbench vanish and return when unhidden |
| PLUG-009 | Missing-module handling on the server: a feature whose namespaced type has no loaded module validates structurally (base fields, `version`, JSON `params` under a size cap), is kept unchanged through PUT, undo, save and reload, and evaluates to `status: "error"` with `Requires module <id>` while keeping the pre-failure state; later features show their usual errors. Extension data for a missing module round-trips untouched. The document stays editable. | todo | PLUG-004, REG-023 | `shared/src/featureSpec.ts`, `server/src/api/validate.ts`, `server/src/geometry/features.ts`, `server/test/missingModule.test.ts` | `npm test -w server -- missingModule` (fixture document with an unloaded `acme.gear` feature and extension data: opens, evaluates with that error, survives PUT and reload as equal JSON values; editing another feature still works) |
| PLUG-010 | Missing-module display: a timeline chip for a missing-module feature shows a puzzle icon and `Requires module <id>`, reopening it shows its params read-only with Delete (confirmed), and the model tree lists `Unavailable: <id> (data kept)` for extension data without a module. Not: new visual design beyond the existing error chip styles. | todo | PLUG-009, PLUG-005 | `client/src/components/Timeline.tsx`, `client/src/components/ModelTree.tsx`, `client/src/features/missing.tsx`, `client/test/missingModule.test.tsx` | `npx vitest run client/test/missingModule.test.tsx` in the DOM lane |
| PLUG-011 | Module data namespaces and migrations: the manifest declares a data version; a module's server entry exports migrations keyed by from-version for its `extensions[<id>]` data and its feature versions; the host registers them with DOC's per-namespace runner, which backs up before migrating. Data newer than the installed module is never downgraded: the module's features show as errors and the data is kept. Backups use DOC-027. It registers a blob enumerator with UNDO-010 so module assets are kept. | todo | PLUG-004, DOC-CP, DOC-027 | `server/src/modules/host.ts`, `server/src/store/migrations.ts`, `shared/src/moduleManifest.ts`, `server/test/moduleMigrations.test.ts` | `npm test -w server -- moduleMigrations` (v1 data migrates to v2 on load with a backup; v3 data on a v2 module is kept and flagged) |
| PLUG-012 | Register module settings under the SET convention plugin.<moduleId>.* and group them under the module name. Unknown/disabled module values survive round trips unchanged as JSON values. No separate module.* or user.modules.* validation convention is introduced. | todo | PLUG-004, SET-001 | `server/src/modules/host.ts`, `shared/src/settings.ts`, `client/src/modules/host.ts`, `server/test/moduleSettings.test.ts` | `npm test -w server -- moduleSettings` (a fixture setting resolves with its default, accepts a user value, and survives the module being disabled) |
| PLUG-013 | Licence display: a Modules page in settings lists each module's name, version, author, SPDX licence, status and error from `GET /api/modules`, and shows the licence text served from the module's `LICENSE` at `GET /api/modules/:id/licence`, beside the core third-party notices OPS added. Compose existing patterns under DEC-004. | todo | PLUG-004, OPS-CP | `server/src/api/routes.ts`, `server/src/modules/host.ts`, `client/src/components/ModulesPage.tsx`, `client/test/modulesPage.test.tsx`, `server/test/moduleHost.test.ts` | `npx vitest run client/test/modulesPage.test.tsx` in the DOM lane; `npm test -w server -- moduleHost` (licence route returns the fixture text; unknown id 404) |
| PLUG-014 | Third-party server loading: the host scans `ROCKETT_PLUGIN_DIR` (default `<DATA_DIR>/plugins`) for `<id>/manifest.json` and `server.mjs`, validates the manifest, and loads a plugin only when the admin enabled it and its folder sha256 matches the recorded one; everything else is listed with status and reason. No route accepts a plugin upload. Hash the complete immutable code/dependency closure that will execute. Reject symlinks and paths escaping the plugin root. Activate a staged immutable copy so changing files between approval and import cannot run unapproved bytes. Trusted code is not sandboxed; a module error boundary does not isolate process or page access. | todo | DEC-501, PLUG-007, PLUG-013 | `server/src/modules/thirdParty.ts`, `server/src/modules/host.ts`, `server/src/index.ts`, `server/test/fixtures/plugins/hello/*`, `server/test/thirdParty.test.ts` | `npm test -w server -- thirdParty` (fixture `hello` loads when enabled with its hash; an edited file disables it; an incompatible range is listed, not loaded) |
| PLUG-017 | Loading-strategy spike. Build the production client with a fixture `client.mjs` loaded by `import()` two ways: (a) an import map, where the host emits ESM entries for `react`, `react/jsx-runtime`, `react-dom/client` and `three` (host-built ESM facades for React, which ships CommonJS only), imports them itself through bare specifiers kept external, and declares the map in `index.html` before the entry script; (b) `@module-federation/vite` shared singletons. For each, record React, react-dom and three identity between host and plugin, a plugin hook rendering inside the host tree, `instanceof THREE.Object3D` across the boundary, client bytes and first-load time. Record the choice and the plugin build contract in the Third-party loading section of `modules.md`. Choosing (b) adds a dependency and becomes a DEC row. | todo | PLUG-005, OPS-030 | `client/vite.config.ts`, `client/index.html`, `client/test/fixtures/plugins/identity/client.mjs`, `client/test/browser/pluginIdentity.browser.test.ts`, `docs/internals/modules.md` | `npm run test:browser -- pluginIdentity` passes on the chosen production build and fails when the fixture bundles its own React; the doc section names the strategy |
| PLUG-015 | Third-party client loading uses the PLUG-017 strategy. The server serves an enabled plugin's `client.mjs` read-only from its PLUG-014 staged copy, to signed-in users only, while its hash matches. The client host imports enabled plugins after first-party modules, and a failed import marks that plugin failed without blocking the rest. | todo | PLUG-014, PLUG-017 | `client/index.html`, `client/vite.config.ts`, `client/src/modules/thirdParty.ts`, `server/src/index.ts`, `server/test/fixtures/plugins/hello/client.mjs`, `client/test/thirdPartyLoad.browser.test.ts` | `npm run test:browser -- thirdPartyLoad`: fixture `hello` enabled renders its command through the host React; an edited `client.mjs` is not served; a throwing import leaves the app usable |
| PLUG-016 | Plugin author guide `docs/user/plugins.md`: layout, manifest fields, API version policy, the contribution points with one example each, data namespaces and migrations, settings, licence, install by folder drop and enable, the trust statement from DEC-501, and the `hello` fixture as the worked example. | todo | PLUG-015 | `docs/user/plugins.md`, `shared/test/pluginGuide.test.ts` | `npm test -w shared -- pluginGuide` (extracts the guide's manifest example and passes it through `parseManifest`) |
| PLUG-CP | PLUG integration checkpoint: every required dependency has passed its focused proof; npm run check and this phase's aggregate checks pass. Update only the existing documentation whose facts changed, and record a sanitised evidence summary. This is not approval to deploy or a claim that excluded scope is complete. Explicitly excluded: PLUG-014, PLUG-017, PLUG-015, PLUG-016. They remain outstanding until completed or explicitly deferred by Mark. | todo | PLUG-001, PLUG-002, PLUG-003, PLUG-004, PLUG-005, PLUG-006, PLUG-007, PLUG-008, PLUG-009, PLUG-010, PLUG-011, PLUG-012, PLUG-013 | `ARCHITECTURE.md`, `API.md`, `CAD_MODEL.md`, `README.md`, `WORK-ORDER.md` | npm run check; scripts/check-work-order.sh; all required phase checks pass with evidence; each excluded row is reported by ID |

## CAM, CNC and CAM

| ID | Outcome | Status | Depends | Scope | Check |
| --- | --- | --- | --- | --- | --- |
| DEC-610 | Mark identifies the first machine/controller and qualifies that exact post and machine profile. Pluggable posts means extensibility, not universal controller compatibility. Other dialects remain candidate exports until independently verified. Keep actual machine identity, offsets and operational evidence outside the public repository. | decision | - | `WORK-ORDER.md` | the ruling is recorded under Rulings |
| CAM-001 | `docs/internals/cam.md` fixes the CAM design before code. It covers: data in `extensions["rockett.cam"]` v1 (tools, setups, operations, machine and post choice per setup); IR v1 (typed, absolute, modeless, mm, absolute arc centres, semantic drill cycles, move roles, `raw` scoped to one post); the pipeline (region extraction in the kernel worker, toolpaths from clipper2-ts offsets, OCCT `BRepOffsetAPI_MakeOffset` for arc-true contours as at `server/src/geometry/features.ts:1028`, capability normaliser, safety checks, declarative post); the body fingerprint rule; toolpath cache location, byte cap and eviction; units per DEC-612 (mm in documents, libraries and IR; inch converted in the normaliser; one inch golden in CAM-026); tool change per DEC-611; per-user libraries per DEC-613; server-side toolpath jobs per DEC-614; consumed bodies keep their operations with `missing reference`; the machine-safety rule (the first run on a machine is an air cut); licence rules (clipper2-ts BSL-1.0; no Kiri:Moto code, whose per-file All Rights Reserved headers contradict its MIT licence; no Fusion posts, which are proprietary; OpenCAMLib and adaptive clearing deferred); and a Manufacture workbench wireframe. | todo | PLUG-CP, DEC-611, DEC-612, DEC-613, DEC-614 | `docs/internals/cam.md` | the note exists, covers every item in this Outcome and cites DEC-611 to DEC-614 by ID; `scripts/check-work-order.sh` passes |
| DEC-615 | Mark approves the Manufacture workbench wireframe in `docs/internals/cam.md`. | decision | CAM-001 | `WORK-ORDER.md` | the approval is recorded under Rulings |
| CAM-002 | `modules/cam` registers as first-party module `rockett.cam` through the PLUG manifest. It adds an empty Manufacture workbench and extension namespace `rockett.cam` v1 `{setups: [], tools: []}` with a schema validator and a migration hook. Invalid CAM data is rejected with 400 and valid data round-trips through save, load and undo. | todo | CAM-001, PLUG-CP | `modules/cam/**`, `modules/cam/test/camExtension.test.ts` | `npx vitest run modules/cam/test/camExtension.test.ts` |
| CAM-003 | Invalidate CAM from a conservative identity of the complete generating inputs: relevant feature dependency closure, source blob hashes, placements, selected topology, kernel/naming versions and CAM version. Volume, area, bbox and face names are diagnostics, never a sufficient shape fingerprint. Prefer an unnecessary regeneration over a false fresh toolpath. A narrower geometry fingerprint is allowed only with evidence that it distinguishes all relevant geometry. | todo | REF-CP | `server/src/geometry/fingerprint.ts`, `server/test/bodyFingerprint.test.ts` | bodyFingerprint tests move equal-volume internal holes while preserving outer bbox and names and require invalidation; a relevant source/placement/version change invalidates; no-op rebuilds may reuse only proven identical inputs |
| CAM-004 | `modules/cam/src/shared/setup.ts` exports `Setup`, `Stock` (`boxAround` with six margins, `box`, `cylinder`), `Wcs` (origin as `stockCorner` or a REF reference plus offset, axes, `offsetIndex` where 1 is G54) and `stockBox(setup, bodyBoxes)`, which returns stock extents in the setup frame using the KIT placement type. Setup bodies are referenced by stable body id. Include safe retract/clearance heights, machining Z direction, workholding/fixture exclusions and explicit WCS-to-machine mapping. Unknown machine offsets cannot produce a claim that machine travel limits were verified. | todo | CAM-002, KIT-CP | `modules/cam/src/shared/setup.ts`, `modules/cam/test/setup.test.ts` | `npx vitest run modules/cam/test/setup.test.ts` |
| CAM-005 | Command `rockett.cam.setup` opens a KIT form-kit dialog for bodies, stock kind, margins, WCS corner and offset index, and saves the setup as one transaction. REG viewport layer `rockett.cam.stock` draws the stock wireframe and the WCS triad. | todo | CAM-004, DEC-615, OPS-CP | `modules/cam/src/client/**`, `modules/cam/test/setupDialog.test.ts` | `npx vitest run modules/cam/test/setupDialog.test.ts` |
| CAM-006 | `Tool` (flat, ball, bull, vbit, drill, chamfer; diameter, corner radius, tip angle, flute length, overall length, shank, flutes, centre cutting) and `Preset` (rpm, cut, plunge and ramp feeds, stepdown, stepover fraction, coolant) live in the DEC-613 library store behind module routes. Using a tool copies it into the document with `libraryRef`. | todo | CAM-002, DEC-613 | `modules/cam/src/shared/tools.ts`, `modules/cam/src/server/**`, `modules/cam/test/toolLibrary.test.ts` | `npx vitest run modules/cam/test/toolLibrary.test.ts` |
| CAM-007 | A Manufacture panel lists, adds, edits and deletes tools with the KIT form kit and SET display units. | todo | CAM-006, DEC-615, OPS-CP | `modules/cam/src/client/**`, `modules/cam/test/toolPanel.test.ts` | `npx vitest run modules/cam/test/toolPanel.test.ts` |
| CAM-008 | `MachineProfile` (name, X, Y and Z min and max, max feed per axis, rpm range, tool-change mode per DEC-611, default post id, output units) lives in the same store as tools and is edited through a form generated from its schema. | todo | CAM-006, DEC-611 | `modules/cam/src/shared/machine.ts`, `modules/cam/src/server/**`, `modules/cam/src/client/**`, `modules/cam/test/machine.test.ts` | `npx vitest run modules/cam/test/machine.test.ts` |
| CAM-009 | `modules/cam/src/shared/ir.ts` exports `Program`, `Section`, `Move` (rapid, feed with role, arc with absolute centre and plane, cycle, dwell, comment, stop, raw) with `irVersion: 1`. `validateProgram` rejects non-finite coordinates, feed of 0 or less, arcs whose start and end radii differ by more than 1e-4 mm, and unknown tool ids. `programStats` returns cut length and estimated time. | todo | CAM-002 | `modules/cam/src/shared/ir.ts`, `modules/cam/test/ir.test.ts` | `npx vitest run modules/cam/test/ir.test.ts` |
| CAM-010 | Kernel job `rockett.cam.regions` finds floor levels from planar faces whose normal is parallel to setup Z and sections the body with `BRepAlgoAPI_Section` at a Z to return closed loops in setup XY with arcs kept as arcs. It runs in the PERF kernel worker. | todo | CAM-004, PERF-CP | `modules/cam/src/kernel/regions.ts`, `modules/cam/test/regions.test.ts` | `npx vitest run modules/cam/test/regions.test.ts`: a 60 by 40 by 10 block with a 20 by 10 by 5 pocket gives floors at 10 and 5 and one pocket loop of area 200 |
| CAM-011 | `clipper2-ts` (BSL-1.0, no dependencies at 2.0.1-18 today; re-read when the row runs) is added exact-pinned to `modules/cam/package.json` per PLUG-001, installed with scripts disabled, listed in `THIRD-PARTY-NOTICES.md`, and its tree read. It reaches the image only inside the bundles. `offsetLoops` and `subtractLoops` in `modules/cam/src/toolpath/geometry.ts` wrap it with a fixed integer scale. The commit body carries the complexity report. | todo | CAM-002 | `modules/cam/package.json`, `package-lock.json`, the licence notices file, `modules/cam/src/toolpath/geometry.ts`, `modules/cam/test/offsets.test.ts` | `npx vitest run modules/cam/test/offsets.test.ts`: a 20 mm square offset by -2 has area 256 within 1e-6; an island subtracts; `scripts/check-notices.sh` passes; `git grep -n clipper2-ts -- server/package.json client/package.json` prints nothing; `moduleBoundaries` passes |
| CAM-012 | Operation `rockett.cam.facing` emits an IR section: zigzag across the stock top outline widened by the tool radius, at the preset stepover, with stepdowns from stock top to model top. | todo | CAM-006, CAM-009, CAM-011, CAM-004 | `modules/cam/src/toolpath/facing.ts`, `modules/cam/test/facing.test.ts`, `modules/cam/test/golden/ir/facing.json` | `npx vitest run modules/cam/test/facing.test.ts` matches the golden IR for 100 by 50 stock and a 6 mm tool |
| CAM-013 | Operation `rockett.cam.contour` offsets a chosen loop outside or inside by the tool radius (OCCT offset for arc-true loops, clipper2-ts otherwise), cuts in depth passes by stepdown, climb or conventional, and plunges at a start point. Not: tabs, lead arcs, `G41` and `G42` compensation. Every operation proves safe entry and retract with the selected tool. Reject a plunge when the tool cannot plunge or the geometry cannot accommodate the entry; do not emit a path on a best-effort basis. | todo | CAM-009, CAM-010, CAM-011, CAM-006 | `modules/cam/src/toolpath/contour.ts`, `modules/cam/test/contour.test.ts`, `modules/cam/test/golden/ir/contour.json` | `npx vitest run modules/cam/test/contour.test.ts` |
| CAM-014 | Contour gains holding tabs as Z lifts: a count, width and height, placed evenly along the last passes and away from corners. Tabs retain their height on every pass that would otherwise cut below the tab top. The check must cover multiple final-depth passes, not only the last pass. | todo | CAM-013 | `modules/cam/src/toolpath/contour.ts`, `modules/cam/test/contourTabs.test.ts` | contourTabs tests verify four spans of the requested width/height remain intact across all passes below tab top and that no connecting move cuts through them |
| CAM-015 | Operation `rockett.cam.pocket` clears a region with inward offset rings around islands, links inside cleared area without retracting, enters by helix when the radius fits and by ramp otherwise, and steps down to each floor. Not: rest machining, adaptive clearing. A link may stay down only inside proven cleared volume at that depth. If a valid ramp/helix cannot fit or a fixture is unmodelled in the path, reject generation or require an explicit safe retract; no silent unsafe fallback. | todo | CAM-013 | `modules/cam/src/toolpath/pocket.ts`, `modules/cam/test/pocket.test.ts`, `modules/cam/test/golden/ir/pocket.json` | `npx vitest run modules/cam/test/pocket.test.ts`: golden IR, and no cut move leaves the region shrunk by the tool radius |
| CAM-016 | Operation `rockett.cam.drill` finds cylindrical faces (as `measure.ts:39` detects them) with axis parallel to setup Z, groups them by diameter, matches drill tools within 0.01 mm, orders points nearest-neighbour from the WCS origin, and emits IR `cycle` drill or peck moves. | todo | CAM-009, CAM-010 | `modules/cam/src/kernel/holes.ts`, `modules/cam/src/toolpath/drill.ts`, `modules/cam/test/drill.test.ts` | `npx vitest run modules/cam/test/drill.test.ts`: a plate with two 3 mm and two 5 mm holes gives two cycles of two points |
| CAM-017 | Job `rockett.cam.generate` runs an operation in the kernel worker with progress and cancel. Its input fingerprint is sha256 over the setup body fingerprints, stock, WCS, operation params, tool snapshot and CAM engine version. The Program is cached under the data folder at `cache/cam/<fingerprint>.json` with a byte cap and least-recently-used eviction, and `lastGenerated` is written through a transaction. Include generator/post capability versions where they affect generation. Compare the captured document revision and input identity again before publishing the result; edits during generation leave the result stale. Cache eviction never removes the only record needed to prove which inputs produced an export. | todo | CAM-003, CAM-012, DEC-614, UNDO-CP | `modules/cam/src/server/**`, `modules/cam/test/generate.test.ts` | `npx vitest run modules/cam/test/generate.test.ts`: a second generate with the same inputs does not call the kernel; exceeding the cap evicts the oldest entry |
| CAM-018 | `operationStatus` returns `fresh`, `stale`, `never`, `error` or `missingReference`, and a module route returns it for every operation. CAM always reads the end-of-timeline model, whatever the Design rollback. Regeneration stays manual. Any failed, cancelled, unresolved or missing-module feature affecting setup geometry blocks generation/export. End-of-timeline evaluation is not permission to use the last valid pre-failure body. | todo | CAM-017 | `modules/cam/src/server/**`, `modules/cam/test/staleness.test.ts` | `npx vitest run modules/cam/test/staleness.test.ts`: editing the setup body's extrude makes the operation `stale`, editing another body leaves it `fresh`, and consuming the body gives `missingReference` |
| CAM-019 | The Manufacture browser shows setups and operations with status badges and supports generate, generate all stale, reorder and suppress, each as one transaction. | todo | CAM-005, CAM-018 | `modules/cam/src/client/**`, `modules/cam/test/browser.test.ts` | `npx vitest run modules/cam/test/browser.test.ts` |
| CAM-020 | Operation dialogs for facing, contour, pocket and drill come from each operation's param schema through the KIT form kit, with geometry picks through REG selection kinds and tool and preset pickers. | todo | CAM-007, CAM-015, CAM-016, CAM-019 | `modules/cam/src/client/**`, `modules/cam/test/opDialogs.test.ts` | `npx vitest run modules/cam/test/opDialogs.test.ts` |
| CAM-021 | `programToSegments(program)` returns positions, per-vertex role colours (rapid, cut, plunge, link) and per-section ranges, with arcs split to 0.01 mm chord error. REG layer `rockett.cam.toolpaths` draws the selected operation or setup, a scrub slider sets `drawRange`, and teardown uses the KIT dispose helpers. | todo | CAM-009, CAM-019 | `modules/cam/src/client/**`, `modules/cam/test/toolpathPreview.test.ts` | `npx vitest run modules/cam/test/toolpathPreview.test.ts` |
| CAM-022 | `simulateHeightmap(program, stock, cellMm)` runs in a Web Worker, sweeps flat, ball and V tool profiles over a Float32Array height grid, and the result draws as a displaced surface on `rockett.cam.stock`. Not: gouge check against the part, tri-dexel. | todo | CAM-021, PERF-001 | `modules/cam/src/client/**`, `modules/cam/test/heightmap.test.ts`, `modules/cam/test/heightmap.bench.ts`, `docs/internals/performance.md` | `npx vitest run modules/cam/test/heightmap.test.ts`: a 10 mm flat pass at depth 2 lowers exactly the swept cells; bench `cam-heightmap` on the PERF-001 harness (300 by 300 mm stock, 0.5 mm cells, a committed synthetic 50k-move program; Node 24 in the pinned image, pure sweep only; 2 warm-up and 10 measured runs) has median at most 5000 ms and p95 at most 6000 ms on the anonymous hardware class in `performance.md` |
| CAM-023 | `normalise(program, post, options)` turns a validated mm IR into a checked `NormalisedProgram` for one post. Arcs, cycles and tool changes (DEC-611) are kept or expanded by the post capabilities. Per DEC-612, coordinates, feeds and depths are converted here to the output unit the machine profile or post options select, and `units` is stamped on the result; the IR type never carries inch. Reject raw moves meant for another post. Prove arc extrema and expanded cycles keep path and safety semantics within stated tolerances. | todo | CAM-009, DEC-611, DEC-612 | `modules/cam/src/post/normalise.ts`, `modules/cam/test/normalise.test.ts` | normalise tests cover full-circle and quadrant extrema, expanded peck cycles, a 25.4 mm move becoming 1 in with `units: "inch"` while the input IR stays deep-equal, incompatible raw rejection, and path error within 0.005 mm |
| CAM-024 | checkProgram rejects unsupported tool/operation combinations, non-finite values, invalid feed/rpm, absent spindle state, unsafe entry/retract, rapid motion intersecting stock or declared fixtures, and missing termination. Check the swept tool envelope and complete arc/cycle paths, not endpoints alone. Validate machine travel only with known WCS-to-machine mapping; otherwise mark that check unverified and block machine-qualified output. Heightmap preview is not collision or gouge proof. | todo | CAM-008, CAM-023, CAM-004 | `modules/cam/src/post/check.ts`, `modules/cam/test/checkProgram.test.ts` | one rejecting fixture per rule, including an arc with safe endpoints but an out-of-range midpoint, rapid through stock, holder/fixture clearance, unknown machine offset and inch/mm mismatch |
| CAM-025 | A declarative JSON post schema (id, label, extension, capabilities, number formats per word, templates for header, footer, tool change, spindle, coolant, rapid, linear, arcs, each cycle kind, dwell and comment, and a modal word list) and `formatProgram(program, post, options)` interpret posts with modal suppression and no code execution. Posts register as the PLUG `postProcessors` contribution. Template expansion cannot inject arbitrary words through names/comments/options. Bound output size and validate the formatted program against the selected dialect. Keep offsets, tool-length compensation and modal reset rules explicit for each qualified post. `formatProgram` never scales numbers; it writes G20 or G21 from the normalised program's units. | todo | CAM-023, PLUG-002 | `modules/cam/src/post/format.ts`, `modules/cam/src/post/schema.ts`, `modules/cam/test/formatProgram.test.ts` | `npx vitest run modules/cam/test/formatProgram.test.ts` with a minimal fake post; an inch normalised program prints unscaled values after G20 |
| CAM-026 | `modules/cam/posts/grbl.json` targets GRBL 1.1: incremental IJK, no canned cycles, no `M6`, parenthesis comments. Golden `.nc` files cover facing, contour, pocket and drill fixture programs byte for byte. Plus golden `contour-inch.nc`, the DEC-612 inch proof. | todo | CAM-025 | `modules/cam/posts/grbl.json`, `modules/cam/test/golden/grbl/*.nc`, `modules/cam/test/postGrbl.test.ts` | `npx vitest run modules/cam/test/postGrbl.test.ts` |
| CAM-027 | `modules/cam/posts/linuxcnc.json` targets LinuxCNC: `G81` and `G83` cycles, `T` then `M6`, `G43 H`, `M2` end. Goldens cover the same four fixture programs. | todo | CAM-025 | `modules/cam/posts/linuxcnc.json`, `modules/cam/test/golden/linuxcnc/*.nc`, `modules/cam/test/postLinuxcnc.test.ts` | `npx vitest run modules/cam/test/postLinuxcnc.test.ts` |
| CAM-028 | An NC program dialog picks setups, post, machine and post options. The server runs normalise, `checkProgram` and `formatProgram` and returns `.nc` files (a zip when split per tool) through the EXCH exporter registry, with a header comment naming the Rockett version, post id and input fingerprint. Stale operations or check errors block export and offer generate first. Validate canonical mm IR before formatting and independently parse/check the emitted dialect afterwards, including rounding, modal state, units and tool changes. Goldens alone cannot catch a consistently wrong generator and expected file. | todo | CAM-020, CAM-024, CAM-026, EXCH-CP | `modules/cam/src/server/**`, `modules/cam/src/client/**`, `modules/cam/test/ncExport.test.ts` | `npx vitest run modules/cam/test/ncExport.test.ts`: a stale operation returns 409 and a clean setup returns the GRBL golden text |
| CAM-030 | Declarative posts for grblHAL (`G81` to `G83`, `M6` per config) and FluidNC (no canned cycles, `M6` per config), each with goldens for the four fixture programs. | todo | CAM-026 | `modules/cam/posts/grblhal.json`, `modules/cam/posts/fluidnc.json`, `modules/cam/test/golden/**`, `modules/cam/test/postGrblFamily.test.ts` | `npx vitest run modules/cam/test/postGrblFamily.test.ts` |
| CAM-031 | Declarative posts for Mach3 and Mach4 (canned cycles, `M6` macro) and Marlin (XY arcs only, no cycles, no tool change, semicolon comments), each with goldens for the four fixture programs. | todo | CAM-026 | `modules/cam/posts/mach.json`, `modules/cam/posts/marlin.json`, `modules/cam/test/golden/**`, `modules/cam/test/postMachMarlin.test.ts` | `npx vitest run modules/cam/test/postMachMarlin.test.ts` |
| CAM-032 | The post for the DEC-610 controller. If the dialect matches CAM-026 or CAM-027, reuse that post; otherwise add a declarative post here with the four fixture goldens and remove that dialect from CAM-030 or CAM-031. Check every emitted word against the controller's published documentation: units, arc form, cycles, DEC-611 tool change, work and tool offsets, modal reset and program end. The repo names only the dialect; machine identity, offsets and profile values stay private. | todo | CAM-028, DEC-610 | `modules/cam/posts/*.json`, `modules/cam/test/golden/**`, `modules/cam/test/postQualified.test.ts`, `docs/internals/cam.md` | `npx vitest run modules/cam/test/postQualified.test.ts` (goldens match byte for byte); `cam.md` cites a documentation section for each checked word |
| CAM-033 | For the DEC-610 controller, `scripts/cam-qualify-pack.mjs <outDir>` writes the reference part program, its sha256 and the post id and version to a directory outside the repo, and `docs/internals/cam.md` gains a generic air-cut checklist: program and post hashes match, work and tool offsets, Z clearance above stock, travel limits, spindle and tool-change handling, feed hold and e-stop. No machine identity or offsets in the repo. | todo | CAM-028, CAM-032, DEC-610 | `scripts/cam-qualify-pack.mjs`, `modules/cam/test/qualifyPack.test.ts`, `docs/internals/cam.md` | `npx vitest run modules/cam/test/qualifyPack.test.ts` |
| CAM-029 | After exact machine-action approval, Mark performs the controller-specific simulation and controlled air-cut procedure for the reference part using the CAM-032 post and the CAM-033 pack. Verify work and tool offsets, safe clearance, travel, spindle/tool-change handling and stop behaviour. Raising Z alone is not the acceptance procedure. Retain the exact post/program hashes and operational evidence privately; publish only a sanitised qualification result. | operator | CAM-028, DEC-610, CAM-032, CAM-033 | private operator evidence outside this repository; `WORK-ORDER.md` status only | the operator records pass/fail for the exact program, post version and approved procedure; a fail keeps this row unfinished |
| CAM-CP | CAM integration checkpoint: every required dependency has passed its focused proof; npm run check and this phase's aggregate checks pass. Update only the existing documentation whose facts changed, and record a sanitised evidence summary. This is not approval to deploy or a claim that excluded scope is complete. Explicitly excluded: CAM-029, CAM-030, CAM-031, CAM-033. They remain outstanding until completed or explicitly deferred by Mark. | todo | CAM-001, CAM-002, CAM-003, CAM-004, CAM-005, CAM-006, CAM-007, CAM-008, CAM-009, CAM-010, CAM-011, CAM-012, CAM-013, CAM-014, CAM-015, CAM-016, CAM-017, CAM-018, CAM-019, CAM-020, CAM-021, CAM-022, CAM-023, CAM-024, CAM-025, CAM-026, CAM-027, CAM-028, CAM-032 | `ARCHITECTURE.md`, `API.md`, `docs/internals/cam.md`, `WORK-ORDER.md` | npm run check; scripts/check-work-order.sh; all required phase checks pass with evidence; each excluded row is reported by ID |

## PCB bridge

| ID | Outcome | Status | Depends | Scope | Check |
| --- | --- | --- | --- | --- | --- |
| PCB-001 | `docs/internals/kicad-bridge.md` fixes the bridge design. It covers: `extensions["rockett.kicad"]` links (source kind, sha256, format and generator version, snapshot asset, outline owner); the `kicad.board` feature; the coordinate map (x, -y); keying on layer names, never ordinals, which moved between KiCad 8 and 9; component bodies keyed on footprint uuid; placeholder boxes from courtyard bounds; component bodies as reference bodies excluded from mesh export and CAM stock by default; licence rules (no KiCad or StepUp code; KiCad library models are CC-BY-SA and never ship in the image); a reference test-board spec for fixtures; and what waits (direct Edge.Cuts edits, a KiCad IPC Link plugin, IDF and IDX). | todo | PLUG-CP | `docs/internals/kicad-bridge.md` | the note exists and covers every item in this Outcome; `scripts/check-work-order.sh` passes |
| PCB-002 | `modules/kicad` registers as module `rockett.kicad`. `parseSexpr(text)` tokenizes S-expressions (quoted strings with escapes, numbers kept as source text, unknown tokens kept) with accessors `child`, `children`, `num` and `str`. Malformed input raises a typed error with line and column. | todo | PCB-001, PLUG-CP | `modules/kicad/**`, `modules/kicad/test/sexpr.test.ts` | `npx vitest run modules/kicad/test/sexpr.test.ts`: a property test shows random trees print and reparse unchanged |
| PCB-003 | `readBoard(tree)` checks `version` against the DEC-620 floor (20241229), warns `untested version` above the KiCad 10 fixture, and reads `general.thickness` and the stackup. It chains Edge.Cuts `gr_line`, `gr_arc`, `gr_circle` (its `end` is a point on the circle), `gr_rect`, `gr_poly` and footprint `fp_*` Edge.Cuts items into loops within 1e-3 mm, with y mapped to -y. Loops are classified by containment and orientation: the one outermost loop is the outline and loops directly inside it are cutouts. Disjoint pieces, an island inside a cutout, open chains and self-intersecting loops are rejected by name. The net table and both `net` forms are skipped. Fixtures are hand-written from the KiCad 9 and 10 writer source, not copied from KiCad demos. | todo | PCB-002, DEC-620 | `modules/kicad/src/board.ts`, `modules/kicad/test/fixtures/*.kicad_pcb`, `modules/kicad/test/boardOutline.test.ts` | `npx vitest run modules/kicad/test/boardOutline.test.ts`: 9 and 10 fixtures of one board give equal loops; arc, nested-cutout and back-side footprint edge cases read correctly; two disjoint outlines, an island and an open chain each fail with their error; a version below 20241229 fails naming KiCad 9; a newer version loads with the warning |
| PCB-004 | `readBoard` adds footprints (uuid, lib id, Reference, Value, side, x, y, angle, attributes such as dnp and exclude_from_bom), pad drills (round, oval with offset, `thru_hole` and `np_thru_hole`), courtyard bounds and `model` entries (path, offset in mm, scale, rotate). Vias are skipped. | todo | PCB-003 | `modules/kicad/src/board.ts`, `modules/kicad/test/fixtures/*.kicad_pcb`, `modules/kicad/test/boardFootprints.test.ts` | `npx vitest run modules/kicad/test/boardFootprints.test.ts`: a footprint at angle 180 keeps pad angles as written |
| PCB-005 | A module route stores an uploaded `.kicad_pcb` as a content-hashed DOC blob, parses it, stores the snapshot JSON as an asset, and writes a link record in `extensions["rockett.kicad"]`. An unsupported version returns 400 naming the floor. | todo | PCB-004, DOC-CP | `modules/kicad/src/server/**`, `modules/kicad/test/upload.test.ts` | `npx vitest run modules/kicad/test/upload.test.ts` |
| PCB-006 | Feature `kicad.board`, registered through the REG feature registry with params `{linkId, placement, options}`, extrudes the outline by the thickness and cuts cutouts, round drills and oval slots. Board faces get stable names (`top`, `bottom`, edge faces) for REF. | todo | PCB-005, REG-CP, REF-CP | `modules/kicad/src/kernel/**`, `modules/kicad/test/boardFeature.test.ts` | `npx vitest run modules/kicad/test/boardFeature.test.ts`: a 50 by 30 by 1.6 board with four 3.2 mm holes has volume 2400 minus 4 times pi times 1.6 squared times 1.6, within 1e-3 |
| PCB-007 | Each footprint becomes a reference body with id `kicad:<linkId>:<footprintUuid>`, named by its Reference, as a courtyard placeholder box of default height 1 mm. Back-side parts sit under the board. These bodies are excluded from mesh export and CAM stock unless the user includes them. Placeholders are labelled approximate and cannot prove component clearance. A fit check involving one reports unknown geometry instead of a false pass. | todo | PCB-006 | `modules/kicad/src/kernel/**`, `modules/kicad/test/componentBodies.test.ts` | `npx vitest run modules/kicad/test/componentBodies.test.ts`: renaming R1 to R7 in the fixture keeps the body id |
| PCB-008 | Users upload model STEP files, stored hashed. A search map resolves `${KICAD9_3DMODEL_DIR}`, `${KICAD10_3DMODEL_DIR}` and `${KIPRJMOD}` paths, and `.wrl` paths try the same-name `.step`. Each STEP is read once per hash through the EXCH importer and placed per footprint (at, then model offset, rotate and scale, in the order the note pins), replacing the placeholder. Missing models are listed. Resolve only uploaded content or explicitly allowed roots; reject traversal, escaped environment substitutions and arbitrary URL/network fetches. | todo | PCB-007, EXCH-CP | `modules/kicad/src/**`, `modules/kicad/test/models.test.ts` | `npx vitest run modules/kicad/test/models.test.ts`: 50 footprints sharing one model read the STEP once; a back-side footprint at angle 90 places its model at the hand-computed matrix within 1e-6 |
| PCB-009 | Embedded models (`kicad-embed://`, ZSTD then Base64 inside the board) decode with Node's built-in `zlib.zstdDecompressSync`, with no new dependency, and feed the same model path. Verify the selected KiCad format and runtime decompression API against primary documentation and real fixtures before implementation. Enforce compressed bytes, decoded bytes, nesting and time budgets before allocation; malformed or oversized content cannot block the main event loop. | todo | PCB-008 | `modules/kicad/src/embedded.ts`, `modules/kicad/test/embedded.test.ts` | `npx vitest run modules/kicad/test/embedded.test.ts` |
| PCB-010 | Re-uploading to an existing link runs `diffBoards(old, new)`, keyed on footprint uuid: added, removed, moved (position or side), model changed, outline changed, thickness changed. Update replaces the snapshot, bodies keep their ids, and references to removed footprints show `missing reference`. Replace link snapshot and generated identities in one durable transaction; undo restores the previous board and asset closure. Include rotated, side-swapped, removed and re-added footprints. | todo | PCB-008 | `modules/kicad/src/diff.ts`, `modules/kicad/src/server/**`, `modules/kicad/test/boardDiff.test.ts` | `npx vitest run modules/kicad/test/boardDiff.test.ts` |
| PCB-011 | A KiCad panel in the Design workbench uploads a board, lists links with status (current, changed, missing model count), shows the diff before Update, and shows the missing-model report. It uses the KIT form kit and REG panel registration with no new visual style. Compose existing patterns under DEC-004. | todo | PCB-010, OPS-CP | `modules/kicad/src/client/**`, `modules/kicad/test/kicadPanel.test.ts` | `npx vitest run modules/kicad/test/kicadPanel.test.ts` |
| PCB-012 | Enclosure fit: `asm.interference` gains a `clearance` in mm and reports pairs closer than it with the minimum distance from `BRepExtrema_DistShapeShape` (as used at `server/src/geometry/measure.ts:111`), board component bodies included. | todo | PCB-007, ASM-016 | `server/src/assembly/interference.ts`, `client/src/assembly/*`, `server/test/enclosureFit.test.ts` | `npm test -w server -- enclosureFit`: a component 0.5 mm from a standoff is flagged at clearance 1 and not at 0.3 |
| PCB-013 | Command `kicad.exportOutline` writes a chosen sketch or planar face loop as DXF through the EXCH DXF writer, with the outline and hole circles on layer `Edge.Cuts` in mm, for import in KiCad by File, Import, Graphics. | todo | PCB-006, EXCH-CP, DEC-622 | `modules/kicad/src/**`, `modules/kicad/test/outlineDxf.test.ts` | `npx vitest run modules/kicad/test/outlineDxf.test.ts`: the DXF parses back to the source loop within 1e-6 |
| PCB-017 | `modules/kicad/test/fixtures/reference/source/` holds a hand-written KiCad 9 reference board with inline fixture footprints and no KiCad library footprint or model: two asymmetric STEP models exported by the EXCH-002 exporter, referenced as `${KIPRJMOD}/models/*.step`, a rotated part, a back-side part and a DNP part. `scripts/make-kicad-fixtures.sh` runs the digest-pinned KiCad 9 and 10 `kicad/kicad` images with `--network none` and a read-only mount: KiCad 9 exports STEP, KiCad 10 runs `kicad-cli pcb upgrade --force` on a copy and exports STEP. `--dry-run` prints both commands and digests without running a container. | todo | PCB-001, DEC-623, EXCH-002 | `modules/kicad/test/fixtures/reference/source/**`, `scripts/make-kicad-fixtures.sh` | `sh scripts/make-kicad-fixtures.sh --dry-run` prints two digest-pinned commands; `git grep -n -e KICAD9_3DMODEL_DIR -e KICAD10_3DMODEL_DIR -e KICAD9_FOOTPRINT_DIR -- modules/kicad/test/fixtures/reference` prints nothing |
| PCB-014 | With Mark's approval per DEC-623, Mark runs `scripts/make-kicad-fixtures.sh`, checks the KiCad 9 and 10 boards and STEP exports, and commits them with both image digests. | operator | PCB-001, DEC-623, EXCH-002, PCB-017 | `modules/kicad/test/fixtures/reference/*` | every `model` path starts with `${KIPRJMOD}/models/`; each STEP's `PRODUCT` names list only the board and the two fixture models; both image digests are committed |
| PCB-015 | The reference board fixtures parse, and component placements from PCB-008 match the oracle STEP within 0.01 mm, including a rotated part and a back-side part. | todo | PCB-008, PCB-014 | `modules/kicad/test/oracle.test.ts` | `npx vitest run modules/kicad/test/oracle.test.ts` |
| PCB-016 | Per DEC-621, a link may use a mounted path under a configured read-only root. Opening the panel checks mtime and sha256 once (no background poll) and shows `changed`. Update still snapshots, so regeneration never reads the live file. | todo | PCB-011, DEC-621, DOC-CP | `modules/kicad/src/server/**`, `modules/kicad/test/mountedSource.test.ts` | `npx vitest run modules/kicad/test/mountedSource.test.ts` |
| PCB-CP | PCB integration checkpoint: every required dependency has passed its focused proof; npm run check and this phase's aggregate checks pass. Update only the existing documentation whose facts changed, and record a sanitised evidence summary. This is not approval to deploy or a claim that excluded scope is complete. Explicitly excluded: PCB-016. They remain outstanding until completed or explicitly deferred by Mark. | todo | PCB-001, PCB-002, PCB-003, PCB-004, PCB-005, PCB-006, PCB-007, PCB-008, PCB-009, PCB-010, PCB-011, PCB-012, PCB-013, PCB-014, PCB-015, PCB-017 | `ARCHITECTURE.md`, `CAD_MODEL.md`, `API.md`, `docs/internals/kicad-bridge.md`, `WORK-ORDER.md` | npm run check; scripts/check-work-order.sh; all required phase checks pass with evidence; each excluded row is reported by ID |

## ELEC, electrical

| ID | Outcome | Status | Depends | Scope | Check |
| --- | --- | --- | --- | --- | --- |
| ELEC-001 | `docs/internals/electrical.md` fixes option (a): nets come from pad nets in `.kicad_pcb` (KiCad 9, board version 20241229, writes a header net table and pads as `(net <code> "<name>")`; KiCad 10, from board version 20251028, writes no table and pads as `(net "<name>")`; the name is the key and codes are ignored); connectors are footprints whose Reference starts with J, P, X or CN, or that the user flags; everything derives from the snapshot, and only user flags are stored, in `extensions["rockett.elec"]` keyed by footprint uuid. ELEC reads board data only through the `rockett.kicad.boardNets` service. | todo | DEC-640, PCB-001 | `docs/internals/electrical.md` | the note exists and covers every item in this Outcome |
| ELEC-002 | The KiCad reader adds pad net names for both grammars. `rockett.kicad` provides the service `rockett.kicad.boardNets(linkId)`, returning nets with members `{footprintUuid, reference, pad}` from the stored snapshot. `rockett.elec` calls it through `services.get`, validates the JSON with its own schema, and shows `Requires module rockett.kicad` when no provider is loaded. Nothing under `modules/elec` imports `modules/kicad`. | todo | ELEC-001, PCB-004 | `modules/kicad/src/board.ts`, `modules/elec/**`, `modules/elec/test/boardNets.test.ts`, `modules/kicad/src/server/**`, `modules/kicad/test/boardNets.test.ts` | `npx vitest run modules/kicad/test/boardNets.test.ts modules/elec/test/boardNets.test.ts`: 9 and 10 fixtures give equal nets; a pad with no net is in none; an absent provider is reported; `moduleBoundaries` passes |
| ELEC-003 | `connectors(snapshot, flags)` lists connectors with pins, nets, 3D pin positions (pad positions through the footprint placement and board placement) and mating direction from the board side. The board side is only a suggested direction. Require explicit connector metadata or a user-set mating frame before asserting mating direction; right-angle and edge connectors invalidate a side-only assumption. | todo | ELEC-002, PCB-007 | `modules/elec/src/**`, `modules/elec/test/connectors.test.ts` | `npx vitest run modules/elec/test/connectors.test.ts` |
| ELEC-004 | An Electrical panel lists nets and connectors with search. Selecting a net highlights its pads on REG layer `rockett.elec.nets`, and selecting a component body shows its Reference and nets. Compose existing patterns under DEC-004. | todo | ELEC-003, OPS-CP | `modules/elec/src/client/**`, `modules/elec/test/netPanel.test.ts` | `npx vitest run modules/elec/test/netPanel.test.ts` |
| ELEC-005 | A connector pin table (Reference, pin, net, x, y, z) exports as CSV through the EXCH exporter registry. | todo | ELEC-003, EXCH-CP | `modules/elec/src/server/**`, `modules/elec/test/pinTable.test.ts` | `npx vitest run modules/elec/test/pinTable.test.ts` |
| ELEC-CP | ELEC integration checkpoint: every required dependency has passed its focused proof; npm run check and this phase's aggregate checks pass. Update only the existing documentation whose facts changed, and record a sanitised evidence summary. This is not approval to deploy or a claim that excluded scope is complete. | todo | ELEC-001, ELEC-002, ELEC-003, ELEC-004, ELEC-005 | `ARCHITECTURE.md`, `API.md`, `docs/internals/electrical.md`, `WORK-ORDER.md` | npm run check; scripts/check-work-order.sh; all required phase checks pass with evidence; each excluded row is reported by ID |

## PRINT, additive as a module

| ID | Outcome | Status | Depends | Scope | Check |
| --- | --- | --- | --- | --- | --- |
| PRINT-001 | `writeStl` and `write3mf` move from `server/src/geometry/exporters.ts` into `modules/additive`, which registers the `stl` and `3mf` exporters through PLUG and the EXCH registry. Shared meshing stays in core. Every STL and 3MF case in `server/test/export.test.ts`, including the EXCH-007 colour cases, moves to `modules/additive/test/meshExport.test.ts`. Output does not change: STL bytes and each unzipped 3MF entry match hashes recorded before the move; the zip container is not compared, because `zipSync` (`exporters.ts:174`) stamps the time. | todo | PLUG-CP, EXCH-CP | `server/src/geometry/exporters.ts`, `server/test/export.test.ts`, `modules/additive/**`, `modules/additive/test/meshExport.test.ts` | `npx vitest run modules/additive/test/meshExport.test.ts` passes with the EXCH-007 cases; `npm test -w server -- export` passes; `git grep -n -e writeStl -e write3mf -- server/src server/test` prints nothing |
| PRINT-002 | Printer profiles live as SET settings in the module namespace: a list of `{id, name, bedX, bedY, height, origin (corner or centre), nozzle}` plus a default printer. They appear in the generated settings panel and follow the user. | todo | PRINT-001, SET-CP, SET-017 | `modules/additive/src/**`, `modules/additive/test/printerSettings.test.ts` | `npx vitest run modules/additive/test/printerSettings.test.ts` |
| PRINT-003 | Command `additive.placeOnBed` picks a planar face and stores a rotation per body in `extensions["rockett.additive"]`, not in the timeline. Export applies it and drops the body to z 0, and clearing it restores the default. Orientation is still a saved, undoable module transaction even though it is not a modelling timeline feature. Preserve relative placement of grouped/mating bodies unless the user explicitly requests separate placement. | todo | PRINT-001, REF-CP | `modules/additive/src/**`, `modules/additive/test/placeOnBed.test.ts` | `npx vitest run modules/additive/test/placeOnBed.test.ts`: the picked face normal is -Z in the exported mesh and the feature list is unchanged |
| PRINT-007 | `docs/internals/design-additive.md` wireframes the build-volume viewport layer and its fit states. The fit text in the export dialog composes existing patterns under DEC-004. | todo | PRINT-003 | `docs/internals/design-additive.md` | the note exists and `npm run lint:writing` passes |
| DEC-661 | Mark approves `docs/internals/design-additive.md` or returns changes. | decision | PRINT-007 | `WORK-ORDER.md` | the approval is recorded under Rulings |
| PRINT-004 | `fitsBuildVolume(bodies, orientation, printer)` checks the oriented bounding box. The export dialog shows a pass or the axis that overflows and by how much, and REG layer `rockett.additive.bed` draws the build volume. Check the final translated/rotated group against the full printer volume and origin convention, not each body independently. A later centring step must not invalidate the earlier fit result. Follow `docs/internals/design-additive.md` as DEC-661 approved it. | todo | PRINT-002, PRINT-003, DEC-661 | `modules/additive/src/**`, `modules/additive/test/buildVolume.test.ts` | `npx vitest run modules/additive/test/buildVolume.test.ts` |
| PRINT-005 | Per DEC-660, Download for slicer writes a 3MF with the oriented bodies centred on the chosen printer's bed, relative placement kept, and the printer name in the file name. Not: network send or a protocol handler. | todo | PRINT-004, DEC-660 | `modules/additive/src/**`, `modules/additive/test/slicerHandoff.test.ts` | `npx vitest run modules/additive/test/slicerHandoff.test.ts`: a two-body fixture on a synthetic 220 by 220 mm printer gives `unit="millimeter"`, two objects, a group centre at (110, 110) within 1e-6, the picked face at z 0 and an unchanged body offset |
| PRINT-006 | Mark opens the PRINT-005 3MF for the committed fixture in one slicer that DEC-660 names, at a recorded version, and confirms mm units, two objects, each object's size within 0.01 mm, the picked face on the bed and the body offset. No real printer profile is used. | operator | PRINT-005 | `WORK-ORDER.md` status and a sanitised evidence line | the operator records slicer, version, the 3MF sha256 and pass or fail per property; a fail keeps the row open |
| PRINT-CP | PRINT integration checkpoint: every required dependency has passed its focused proof; npm run check and this phase's aggregate checks pass. Update only the existing documentation whose facts changed, and record a sanitised evidence summary. This is not approval to deploy or a claim that excluded scope is complete. | todo | PRINT-001, PRINT-002, PRINT-003, PRINT-004, PRINT-005, PRINT-007, PRINT-006 | `ARCHITECTURE.md`, `API.md`, `WORK-ORDER.md` | npm run check; scripts/check-work-order.sh; all required phase checks pass with evidence; each excluded row is reported by ID |
