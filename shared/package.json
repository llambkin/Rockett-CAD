{
  "name": "@rockett/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "bench": "vitest bench --run --reporter=default --reporter=json --outputFile.json=dist/bench.json",
    "test": "tsc -p . && vitest run"
  },
  "dependencies": {
    "typebox": "1.3.34"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vitest": "5.0.1"
  }
}
